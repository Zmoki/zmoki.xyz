---
export interface Props {
  datetime: Date;
}

const { datetime } = Astro.props;
---

<time datetime={datetime.toISOString().substring(0, 10)}
  >{
    datetime.toLocaleDateString("en-US", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }</time
>
