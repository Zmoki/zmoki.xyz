---
// Tech blog homepage
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import { PostCard, type PostCardProps } from "@zmoki/shared-components";

export const publishDate = new Date("2025-09-17");
export const contentModifiedDate = new Date("2025-09-17");

const posts = await getCollection("feed");
const sortedPosts = posts.sort(
  (a: PostCardProps, b: PostCardProps) =>
    b.data.publishDate.getTime() - a.data.publishDate.getTime(),
);
---

<BaseLayout title="Tech Blog - Zmoki" description="Technical insights and development thoughts">
  <section>
    <header>
      <h1>Zarema Khalilova</h1>
      <p>
        As a software architect with over 20 years of experience, I solve complex technical SEO
        challenges. As co-founder of the <a href="https://scalekarma.com">Scalekarma</a> research team
        and an independent consultant, I provide the data-driven strategies and developer-ready roadmaps
        you need to navigate the shift to AI search.
      </p>
    </header>
    <section>
      <h2>Technical SEO & AI Search Consultancy</h2>
      <h3>The Problem I Solve</h3>
      <p>
        The biggest challenge in technical SEO isn't always finding the right strategy - it's
        getting it approved and implemented by busy engineering teams.
      </p>
      <h3>My Solution</h3>
      <p>
        I act as the bridge between marketing and engineering. I translate your goals into
        developer-ready roadmaps and bulletproof, data-driven business cases that get your most
        advanced projects for AI Search unblocked and shipped.
      </p>
    </section>
    <section>
      <h2>Latest Investigations & Builds</h2>
    </section>
    <section>
      <h2>Recent Posts</h2>
      {sortedPosts.map((post: PostCardProps) => <PostCard slug={post.slug} data={post.data} />)}
    </section>
  </section>
</BaseLayout>
